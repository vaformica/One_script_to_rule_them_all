# Behavioral-assay analysis source

The Firebird installer places the current source files directly in this folder:

- `analyze_idtracker_beetle.py`
- `run_idtracker_postprocessing_batch.py`

After installation, these are the pipeline's editable working copies. The
pipeline does not call the old standalone repository at runtime.
