# v0.8.0

Architecture release: canonical deterministic sessions, optional nonfatal archive, postprocess-only mode, editable BA/fight parameters, selection helpers, stage status files, consolidated QC CSVs, track-map collection, and QC HTML gallery.
